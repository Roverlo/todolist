## 目标
- 将应用主色改为绿色，并调整项目选中高亮为绿色系
- 重新打包生成最新版 exe，并删除 portable 目录下旧的 exe

## 修改项
- 将 `App.css` 中 `:root --accent` 从蓝色改为绿色（如 `#16a34a`）
- 将 `.project-item.active` 的边框/背景从蓝色系改为绿色系（如边框 `#86efac`、背景 `#ecfdf5`）

## 构建与交付
- 使用 Tauri 构建 release 版本
- 拷贝到 `C:\personal\task\portable`，命名为 `Task-green-YYYYMMDD-HHmmss.exe`
- 删除旧的 `Task.exe` 及历史 `Task-*.exe`

## 验证
- 启动应用，按钮主色与选中高亮为绿色
- portable 目录仅保留最新绿色 exe