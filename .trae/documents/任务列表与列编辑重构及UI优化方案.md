## 调整列分隔线
- 将任务表每列的分隔线改为黑色且加粗（如 `2px solid #000`），确保在普通列与 pinned 列（sticky）上表现一致
- 保持现有斑马纹整行着色，单元格背景继承行背景，解决“白块”视觉问题

## 项目表整合“汇总/未分类”
- 在侧栏项目表格中新增两行：`汇总` 与 `未分类`
- 名称列可点击、切换任务视图；编辑/归档/删除三列显示 `/` 占位且禁用点击
- 行背景沿用项目区配色方案，区分度一致

## 列编辑功能重设（仅新增/改名）
- 替换现有“列/密度”弹窗为“列编辑”弹窗：
  - 新增列：用户输入自定义列名并添加到 `columnConfig.columns`
  - 编辑列：对任意现有列改名（包括内置列，保留键与显示名的区分）
- 去除“可添加列子任务附件”等不需要的入口与模板/密度相关功能
- 数据模型扩展：
  - 在 `Task` 中新增 `extras?: Record<string, string>`，用于存储自定义列的值
  - 表格渲染/编辑：对未知列（非内置字段）从 `task.extras[col]` 读写
  - 迁移策略：不更改已存在数据；新增列默认空值

## 新增待办弹窗动态同步列
- 遍历 `columnConfig.columns`：
  - 内置字段（如 `title/status/priority/dueDate/onsiteOwner/lineOwner/nextStep/notes`）使用对应控件
  - 自定义列（extras）显示文本输入，并写入 `extras[colName]`
- 保持客户端校验（标题必填≤50）与键盘提交（Ctrl+Enter）
- 布局继续使用两列网格，移动端切为单列

## 表头点击排序实现
- 扩展排序支持：`title/notes/nextStep/latestProgress/project` 与任意自定义列（extras）
- 更新 `toSortable`：
  - `title/notes/nextStep` 返回字符串比较
  - `latestProgress` 返回最新进展的 `note` 或时间戳以便排序
  - 未知列从 `task.extras[key]` 读取
- 单击列头切换 `asc/desc`，显示简易的方向指示（▲/▼）

## 记录进展弹窗重设计
- 顶部一行：时间戳 + 状态单选 + “添加附件”按钮（与状态同行右侧）
- 中部占大面积的进展说明 textarea（更大行数）
- 下方列表展示“全部进展记录”，每条提供“编辑”并在同区域更新
- 维持附件体积校验与乐观更新

## 验证与交付
- 构建产出 MSI/EXE，确认：
  - 列分隔线加粗变黑、整行着色正确
  - 侧栏项目表含“汇总/未分类”且动作禁用
  - 列编辑仅支持新增/改名，且自定义列在“新增待办”弹窗中动态出现并可写入
  - 表头点击排序对所有列生效（含自定义列）
  - 记录进展弹窗布局优化，附件按钮位置与说明区空间满足需求