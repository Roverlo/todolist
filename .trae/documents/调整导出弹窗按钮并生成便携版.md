## 修改范围与目标
- 将“取消”和“导出”按钮整体右对齐到卡片（弹窗）右侧
- 将“取消”按钮改为浅灰样式，以便与背景区分
- 不影响其他弹窗的布局与样式

## 代码修改
1. `c:\personal\task\app\src\components\toolbar\ExportModal.tsx:106–115`
   - 在 `<footer className='create-dialog-footer'>` 内，将按钮容器 `<div className='create-footer-actions'>` 改为：`<div className='create-footer-actions export-footer-actions'>`
   - 将“取消”按钮类从 `btn btn-ghost` 改为 `btn btn-light`
   - 保持“导出”按钮为 `btn btn-primary`
2. `c:\personal\task\app\src\App.css:1095–1127` 附近
   - 新增样式：
     - `.export-footer-actions { justify-content: flex-end; }`
   - 使用现有 `.create-footer-actions { display: flex; gap: 8px; }`，只为导出弹窗增加右对齐，不影响其他弹窗

## 构建与打包
- 前端构建：`npm run build`（在 `c:\personal\task\app`）
- 生成可执行（Tauri）：`npm run tauri:build`
- 产物位置（Windows）：
  - 原始 exe：`c:\personal\task\app\src-tauri\target\release\app.exe`
  - 分发包（便携/安装）：`c:\personal\task\app\src-tauri\target\release\bundle\nsis\*.exe`
- 将生成的 `.exe` 复制到 `c:\personal\task\portable`，用于交付便携版

## 验证
- 启动应用查看导出弹窗：按钮应在右侧排布；“取消”为浅灰；“导出”为主色
- 在 Windows 上运行生成的便携版 `.exe`，确认弹窗样式生效

## 交付
- 修改完成后提供：
  - 更新后的界面效果说明
  - 便携版 `exe` 文件位于 `c:\personal\task\portable`