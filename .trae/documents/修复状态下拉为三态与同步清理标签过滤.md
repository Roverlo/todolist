## 问题定位
- 过滤区的“状态”下拉仍为两项（进行中/已完成），缺少“挂起”。
- 代码确认：`PrimaryToolbar.tsx:100-102`仅渲染两项；其他处（行内编辑、批量操作、新增待办）已是三态。

## 修复方案
- 在`PrimaryToolbar.tsx`的状态下拉补充第三项：`挂起` (value='paused')，对应`filters.status`赋值。
- 同步清理“标签”过滤输入（之前已决定移除标签列与相关过滤），删除`标签`输入块。
- 保持其它过滤与分组逻辑不变。

## 验证
- 构建后在过滤下拉显示三项：进行中/已完成/挂起/全部；选择“挂起”列表正确过滤。
- DSL搜索仍支持`status:paused`；点击列头排序不受影响。
- UI中无“标签”输入。

## 交付
- 应用修复并重新打包EXE/MSI，告知路径。