## 问题

* 顶部出现两个“新建单次任务”按钮；代码中有两套状态与两处弹窗：`addTaskOpen` 和 `addOpen`，分别渲染了两个 `SingleTaskModal`。

## 修复方案

1. 保留 `addTaskOpen` 作为唯一的单次任务开关；删除 `addOpen` 状态、按钮和对应的第二个 `SingleTaskModal`。
2. 顶部顺序保持：

   * 主按钮：新建单次任务（使用 `addTaskOpen`）

   * 次按钮：新建周期任务

   * 次按钮：导出
3. 清理多余 import/变量，避免未使用警告。
4. 构建并交付新的便携版 exe 验证按钮唯一性与交互正常。

## 验证

* 页面仅显示一个“新建单次任务”按钮；点击弹出一次弹窗；关闭后可再次打开；其他按钮正常。

