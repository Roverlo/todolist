## 目标
- 进一步减少横向占用，将“状态/优先级/截止日期”从表头与第1行移至第2行
- 保持信息完整可见、行内可编辑、粘性关键列不变

## UI布局调整
- 第1行（Row Top）：[项目(粘性)] [标题(粘性)] [详情] [最近进展] [下一步] [操作]
- 第2行（Row Bottom）：[状态] [优先级] [截止日期] [现场责任人] [产线责任人] [附件] [创建时间] [自定义列…]
- 第2行字段以“标签+内容”栅格并列显示，长文本换行；数值型/日期型采用胶囊与剩余天数提示

## 交互与编辑
- 行内编辑：
  - 第2行的状态/优先级/截止日期支持下拉与选择器，与现有编辑器共用逻辑
  - 第1行的详情/进展/下一步保持原编辑控件
- 操作列仍统一在第1行右侧；第2行不渲染操作按钮

## 技术实现
- `TaskTable.tsx`
  - 调整 `TOP_COLUMNS`：移除 `status/priority/dueDate`，保留 `project/title/notes/latestProgress/nextStep`
  - 表头渲染改用新的 `TOP_COLUMNS`
  - 第1行渲染同步改为按 `TOP_COLUMNS`
  - 第2行新增三个字段块：状态/优先级/截止日期（沿用 `renderDisplay`/`renderEditor`）
  - 自定义列与责任人/附件/创建时间保留，空值不显示以减少空白
- 样式 `App.css`
  - `row-bottom` 使用 `repeat(auto-fit, minmax(220px, 1fr))` 的栅格；标签 `.label` 保持一致
  - 为状态/优先级/截止日期的胶囊与日期剩余天数提供轻量样式（沿用已有胶囊样式）

## 保留与移除
- 移除列头排序（已移除）；不再在表头显示状态/优先级/截止日
- 粘性列仅 `项目/标题` 保持；其它列不 sticky

## 验证
- 宽屏与窄屏下横向滚动显著减少
- 第2行无内容的字段不占格，栅格自动压缩；编辑态下展示对应控件
- 绿色版构建并运行确认布局紧凑，无大片空白

## 交付
- 更新实现后，构建绿色版到 `C:\personal\task\portable\Task.exe`
- 保留列编辑的新增/删除/改名功能，自定义列在第2行动态呈现

确认后我将开始修改并交付新版绿色版。