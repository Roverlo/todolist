## 目标
- 重新构建当前代码的 release 版本，并在 portable 目录提供最新 exe 文件（保留绿色主题与已移除浮动按钮的改动）。

## 操作
- 执行桌面构建：`npm run tauri:build`
- 将生成的 `src-tauri/target/release/app.exe` 拷贝到 `C:\personal\task\portable`，命名为 `Task-YYYYMMDD-HHmmss.exe`
- 不强制删除历史 exe，避免控制台异常；若需要我可再清理。

## 交付
- 返回完整的 exe 路径与时间，便于你直接运行验证。