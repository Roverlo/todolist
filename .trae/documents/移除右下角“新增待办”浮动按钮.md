## 改动内容
- 删除 `App.tsx` 中右下角绿色浮动按钮（class `fab-button`）的 JSX 片段，保留顶部两个“新建”入口。
- 可选清理：移除 `App.css` 中 `.fab-button` 样式块，避免无用样式。
- 快捷键 Ctrl+N 继续保留（如需一并移除请告知）。

## 影响范围
- 仅涉及 `app/src/App.tsx` 与 `app/src/App.css`，不影响其他功能（导入/导出、周期任务等）。

## 验证
- 页面右下角不再显示浮动按钮；顶部“新建任务/新建周期任务”正常工作。