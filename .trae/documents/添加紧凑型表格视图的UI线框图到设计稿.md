## 目标
- 在 `design.md` 的“信息架构与UI”章节下补充可视化的ASCII线框图，体现左侧项目、顶部工具栏、主区表格、分组切换与详情抽屉。

## 内容
- 主视图（表格密度紧凑）的整体线框图。
- 详情抽屉展开态线框图（默认关闭，仅示意）。
- 多选批量操作条线框图。
- 注解：逾期高亮、状态颜色、列固定、键盘操作提示。

## 文件操作
- 直接在 `c:\personal\task\design.md` 对应章节追加“UI线框图”小节，不创建新文件。

## 验证
- 打开文件检查渲染与对齐；内容与前文字段/交互一致。