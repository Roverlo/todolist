## 基本思路
- 以现有 `design.md` 为主干，融合旧稿的可复用要素（重复规则、筛选/汇总、CSV导出等），形成一份结构完整、可落地、可团队共享的最终设计稿。
- 保持方法论与工具一体化：将 CCFP（Capture/Clarify/Focus/Progress）循环固化为产品功能与约束，确保“轻重缓急”“复盘与分享”问题得到解决。
- 完成后仅保留新的设计稿文件，删除临时与旧版文件。

## 研究结论与整合点
- 现稿亮点：优先级与能量标签、Today 限额、Logbook 复盘、Markdown 导出、隐私分享（见 `c:\personal\task\design.md:11-31, 99-111`）。
- 旧稿可取：重复规则 `RepeatRule`、筛选/汇总 `filters/summaryFilter`、CSV 导出与附件结构（见 `c:\personal\task\temp_old_design.md:46-74`）。
- 整合策略：
  - 数据模型保留现稿的 `Priority/Energy/Status` 与日志结构，扩展 `Task.repeatRule` 与 `AppState.filters`。
  - 导出能力在 Markdown/HTML 基础上补充 CSV（团队报表场景）。
  - 保留隐私脱敏与分享边界，统一到“团队分享”章节。

## 拟定更新内容（设计稿章节）
1. 背景与目标：明确个人痛点与团队分享诉求（覆盖成功指标）。
2. 方法论：CCFP 循环 + 可推广模板与培训方式。
3. 产品原则：简洁、可复制、默认安全（隐私脱敏）。
4. 功能模块：Inbox/看板/Focus/Logbook/Review/分享，补充“筛选/汇总”和“重复任务”。
5. 信息架构与页面：看板+清单+日历的视图切换，加入过滤器与导出入口。
6. 关键流程：晨间梳理、执行-番茄、完成-日志、周月复盘与分享。
7. 数据结构：合并两稿，增加 `RepeatRule` 与 `filters`，扩展 `settings.exportFormat: 'markdown'|'html'|'csv'`。
8. 规则与约束：P1≤3、Today≤7、完成摘要≥20字、附件≤10MB、隐私标签脱敏；重复任务与逾期提示。
9. 存储与导出：本地 JSON + 附件目录 + `exports/`（新增 CSV 文件命名与字段约定）。
10. 非功能与衡量：易用性、可靠性、分享可用性；团队推广方案。

## 文件操作计划
- 保留：更新后的 `design.md`（整合版）。
- 删除：`temp.txt`、`temp_old_design.md`（内容已融入新稿）。

## 验证
- 自检：设计稿章节完整性与一致性检查；数据结构字段一一对应；约束可测试。
- 可用性验证：模拟周/月复盘导出用例（Markdown/CSV）与隐私脱敏样例。

## 交付
- 产出：一份最终的“待办事项”App产品设计稿（覆盖方法论+功能+数据+约束+导出+推广）。
- 清理：删除其他文件，确保目录仅保留最新设计稿。