## 更新目标
- 将此前全部建议并入现有 `design.md`，保持“项目中心+紧凑表格”的极简风格，同时为开发同事提供可直接实现的规格：字段、交互、校验、数据结构、CSV、错误报告、快捷键。

## 更新点
1. 扩展功能与详细规格：保存筛选视图、多列排序与方案保存、Excel批量粘贴新增、导入预览与错误报告、自定义列与列模板、模板行、责任人目录、逾期阈值与颜色策略、子任务/依赖、撤销历史、搜索语法、附件拖拽与预览。
2. 数据结构扩展：`SavedFilter`、`ColumnConfig`、`Dictionary`、`ChecklistItem`、`Dependency`、`HistoryLog` 等。
3. 导入/导出与错误报告规范：字段映射、预览、失败CSV、建议值。
4. 搜索语法：键值+比较符的轻量 DSL 与示例。
5. 快捷键矩阵与交互流程：表格编辑、批量、多选、撤销、分组切换。
6. 验收用例：开发自测的场景清单与预期结果。

## 文件操作
- 仅更新并追加 `c:\personal\task\design.md` 内容，不创建新文件。

## 验证
- 打开文件确认新增章节结构完整、与现有模型一致；示例与规则可直接转为开发任务。