## 结论
- 文档与样式并未要求横向滚动；当前全局样式明确禁止横向滚动：`c:\personal\task\app\src\App.css:18` 设置 `overflow-x: hidden`。
- “横向滚动”曾作为技术兜底建议，但你明确不接受，因此本方案完全按“无横向滚动”优化。

## 证据
- 禁止横向滚动：`app\src\App.css:18` `.app-shell { overflow-x: hidden; }`
- 侧栏“汇总”表格的列宽冲突导致挤压：
  - `ProjectSidebar.tsx:171–176` `<colgroup>` 将右列设为 `60px`
  - `App.css:118–123` `.project-list.compact td:last-child { width: 50px; }` 与上面冲突，叠加 `padding` 导致右列文本挤压

## 修改方案（不引入横向滚动）
1. 统一“汇总”右列宽度与内边距
- 将 `App.css:118–123` 的 `width: 50px` 改为 `60px`，与 `<colgroup>` 一致
- 降低该列 `padding-right`，并设置 `white-space: nowrap; text-overflow: ellipsis; overflow: hidden;`

2. 稳定主区右侧操作可见性（不产生横向滚动）
- 保持 `.app-shell { overflow-x: hidden; }` 不变
- 为主内容容器加入小的右侧安全内边距（12–16px），避免与系统滚动条贴边的视觉遮挡
- 工具栏右侧 `.toolbar-actions` 父级/自身加 `min-width: 0`，允许在窄宽度下正常收缩；文案统一为短中文（“导入”“导出”）

3. 保持操作列贴靠但不被裁剪
- 保留粘性列 `.actions-column`，明确背景色与 `z-index`，避免与邻列/滚动条重叠

## 验证
- 在 1024×600/768/900 下：汇总视图右侧“导入/导出”“编辑/查看更新”均完整可见；无横向滚动条
- 其他项目视图行为不变

## 影响与回滚
- 仅调整侧栏“汇总”表格宽度与内边距及少量布局安全区；风险低、可随时回退