# 随记待办：已确认选图与偏好

记录日期：2026-09-22。适用范围仅为 ProjectTodo 随记中心的标题区、待办统计和正文待办；不推定为其他页面或项目的统一尺寸。

## 用户明确反馈

| 已确认要求 | 来源 |
| --- | --- |
| 先给三张示例图，再按选中的图实施 | 本任务初始要求：“给我3张示例图” |
| 统计标题使用“待办事项”，缩小统计组件、尽量少占空间 | 第一轮批注：“本篇随记改成 待办事项……这个得做小一点，尽量少占用空间” |
| 标题与统计整体上移，把下方空间留给正文 | 第二轮三处批注：“能不能往上挪一点？给下面留出更多空间？”、“也往上挪”、“整体上移” |
| 按最后一张示例实施 | 第三轮批注：“按这个来。” |
| 点击待办排序后，序号随当前位置自动重新编号 | 第三轮补充：“点击待办的排序，序号要自动重新编号” |
| 序号和勾选框更靠近 | 第三轮补充：“另外序号和后面的勾选框稍微间隔近一点” |
| 待办列表前的空行应可删除，保留待办内容、勾选和层级 | 2026-09-22：“前面的那个空行删不掉了”，截图 `codex-clipboard-8020acaf-2076-4546-987d-f3365175ee20.png` |

本地选图来源：`C:/Users/胡文雨/.codex/generated_images/01a0c6a4-a459-7fb3-bcbb-47cb5bbc4def/exec-b3d5049d-792b-4872-903a-9ff39bc2aa1a.png`。这是本机参考文件，不属于仓库资源；以上文字记录在图片不可用时仍可查阅。

## 当前实现与验收状态

- **选定**：上述图片及后续文字批注；旧稿中的“本篇随记”统计标题、大统计区域和较大顶部留白已被替代。
- **已实现**：源码提交 `b7452aeb397e5cb6310604a148966c70fb4b5ad2`。标题与统计同一行，显示总数、已完成、未完成；包含子待办。父子编号根据编辑器中的位置动态计算，不写入正文 HTML。
- **实现尺寸**：顶部内边距 4px；进度环 36px；统计标题、明细和序号 12px；序号至勾选框间距 4px。这些是本次实现选择，不冒充用户逐项指定的数值。
- **已验证**：同次开发的编辑器完整回归、生产网页待办回归、隔离原生 WebView2 待办回归，以及 1536/1186/1100/900px 布局检查通过。900px 仅为网页压力测试。详细范围见 [Windows 验收](RELEASE_QA.md) 及本机 `app/ui-check.local/design-qa.md`。
- **工具限制**：Windows Computer Use 可观察测试窗口，但该轮点击/键盘未可靠触发预期交互；该项记为 LIMIT，原生功能通过来自 AUTO，不宣称 CUA 交互通过。
- **用户接受状态**：用户已经选定设计并要求 EXE；尚未收到用户对实际 EXE 使用效果的验收反馈。

新版 EXE 的哈希、原生运行与重启结果单独记录在 `portable/01_Offline_Portable/ProjectTodo_<构建时间>_QA.md`。不得将这里的同源码旧证据写成对所有新产物的重新验收。

## 前导空行修正（2026-09-22）

第一条待办开头按 Backspace 时，先删除同一容器内紧邻列表的空段落，保留待办；在前面的空段落按 Delete 也可删除空行。Ctrl 组合键行为一致，连续空行逐次删除，支持撤销、重做和保存重开。只在用户执行删除时处理，不自动清理笔记中的空行。

此规则优先于默认列表提升和空待办删除。非空正文沿用原有编辑行为；引用及表格单元格内局部处理，不跨容器。父待办所必需的首段落保留，避免损坏子项结构。

用户报告确认了问题及期望；上述键盘细节是本轮实现选择。专项检查见 `scripts/check-note-leading-blank.mjs`，本轮执行结果记录在本机 `app/ui-check.local/leading-blank-20260922/REPORT.md`，不将修复等同于用户已验收。
