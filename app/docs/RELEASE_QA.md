# Windows 免安装版验收流程

每次发布记录源码提交、EXE 路径、字节数、SHA-256、Windows/WebView 环境、测试日期及证据目录。必须测试待交付的 EXE；修改代码后重新构建，再复测失败场景及关联回归。

## 数据与执行顺序

1. 检查 main 和工作区，确认远程状态及推送权限。默认在 main 构建 `npm run tauri:build -- --no-bundle`（用户明确指定其他分支时遵从该范围），通过 `scripts/create-portable.ps1` 生成单文件 EXE。
2. 备份用户 Documents/ProjectTodo 全目录，逐文件核对 SHA-256，保存恢复命令。测试使用独立 `PROJECTTODO_TEST_DATA_DIR` 和 `WEBVIEW2_USER_DATA_FOLDER`，仅放虚构任务、随记和测试凭据。
3. 使用 computer-use 的窗口枚举、截图/可访问性树、点击和键盘 API 操作 EXE。每次动作后重新观察；不能用旧截图坐标或旧控件索引操作新页面。
4. 运行下表的功能流程；自动化补充边界组合。记录实际执行方式：CUA（Windows 界面）、AUTO（脚本）、LIMIT（环境限制）、FAIL（未解决）。没有执行的检查不能写 PASS。
5. 问题先记录复现步骤、实际结果和预期结果，在当前已授权分支修复，留下最小回归检查。重新执行失败步骤及相关检查；最终源码有变化时重新打包与复核。日常修改验证后提交并推送 main，发布与分发仍需单独授权。
6. 验证退出后测试进程消失、用户数据哈希未变化、交付 EXE 与被测产物哈希一致。保留报告、日志和必要截图；测试记录及备份不提交仓库。
7. 新 EXE 验收通过后，清理仓库 `portable/01_Offline_Portable/`：按 `ProjectTodo_YYYYMMDD_HHmm.exe` 文件名的构建时间降序保留最新 4 个，只删除该目录下更早的匹配 EXE。删除前核对解析后的绝对路径、保留名单及目标未被进程使用；用 PowerShell `Remove-Item -LiteralPath` 逐个删除，不递归、不删除其他文件。记录删除数量、释放空间及最终保留名单。

## 功能检查表

| 编号 | 流程 | 预期与关键边界 |
|---|---|---|
| W01 | 首次启动 | 无安装向导，窗口可交互，无黑屏，加载隔离数据 |
| W02 | 连续启动同一 EXE、改名副本 | 唤醒已有窗口，保持同一主进程，不产生第二个黑窗 |
| W03 | 关闭、取消、最小化到托盘、再次启动 | 取消留在窗口；托盘隐藏；重复打开恢复当前内容 |
| W04 | 记住关闭选择、明确退出、重新启动 | 记忆行为正确，退出终止进程，重启保留数据 |
| W05 | 页面加载并注册关闭监听后，模拟前端忙碌及渲染进程崩溃 | 点原生 X 后约 3 秒出现原生提示；退出结束进程；继续等待后恢复，过期关闭事件不再执行 |
| T01 | 新建/修改项目、任务必填校验 | 合法数据保存，缺少必填项提示，不覆盖其他记录 |
| T02 | 编辑标题、项目、状态、优先级、负责人、截止日期 | 看板与详情一致，重开仍保留 |
| T03 | 子任务添加、修改、完成 | 状态及完成率更新，主任务保持一致 |
| T04 | 填进展直接保存 | 草稿加入进展且只加入一次；指定时间保留 |
| T05 | 手动添加进展再保存、编辑已有进展 | 不重复；编辑原记录；空白忽略；无效日期保留草稿 |
| T06 | 搜索、筛选、清空筛选 | 命中正确；展开筛选不挤动统计/列表 |
| T07 | 排序、列设置、批量操作 | 列表顺序及选中集合正确；关闭重开保留设置 |
| T08 | 周期任务创建/编辑/停用 | 日/周/月配置可保存，生成规则及周日正确 |
| N01 | 新建无标题/有标题随记、日期与树选择、新增当日随记 | 编辑器与 AI 当前随记同步；历史/未来日期归档正确；日期只用于新建和定位，新建及重启不产生日期筛选，其他日期仍可见 |
| N02 | 输入、自动保存、手动保存、快速切换、重启 | 新输入不丢失，切换不串内容 |
| N03 | 随记搜索、标签添加/筛选、清除、定位；从列表菜单增删标签后继续编辑、保存重开 | 搜索匹配可见标题/正文并忽略首尾空格；搜索和标签取交集，条件及空结果可见，一键清除、再次点击标签取消；新建清除条件，定位解除遮挡当前笔记的条件；搜索展开命中分组，标签计数不含回收站；菜单与正文标签同步且不覆盖草稿，短窗口弹窗完整可见 |
| N04 | 回收站移入/恢复 | 只影响测试记录；恢复内容、日期与标签 |
| N05 | 跨天编辑旧随记、同日修改较早的随记、重新启动 | 固定日期不变；无固定日期的旧随记按创建日归档；同日按创建时间稳定排序 |
| E01 | 标题、字体、字号、加粗/斜体/下划线/删除线 | 选区格式正确，撤销/重做与重载保留 |
| E02 | 字体颜色/背景色、70 色盘、HEX、自定义色、无颜色 | 高亮紧贴文字无大圆角；前景色不被覆盖；键盘可操作 |
| E03 | 段落对齐、行距、缩进、项目符号、编号 | 样式作用于当前段落或选区，嵌套与撤销正常 |
| E04 | 待办、回车连续添加、勾选、排序 | 新项未完成；勾选父项完成所有下级并记录时间，已完成子项保留原时间，旧数据不补造时间；取消只影响当前项；同一层级的多个待办列表跨空行和文字统一排序，间隔段落保留原位，子项随父项移动且不跨父项混排；联动和排序可整体撤销/重做，保存重载保留时间 |
| E05 | Tab/Shift+Tab、列表、代码、表格、Escape | 正文插入空白而非跳走；列表缩进、表格导航保留；可退出编辑焦点 |
| E06 | 插入表格、立即输入、行列、合并/拆分、右边缘缩小 | 鼠标选择尺寸后焦点留在新单元格；整表宽度可缩小，内部列独立拖动；保存/撤销/导出保留 |
| E07 | 链接添加/修改、图片上传/粘贴/拖放/路径 | 对话框完整可见；满宽图片选中不撑出横向滚动条，四角可拖拽缩放；图片文件能找到，保存及导出可读 |
| E14 | 任意文件附件、无大小上限图片 | 选择/粘贴/拖入，含 HTML 的图片剪贴板、Windows 资源管理器复制、空文件、未知格式、超 2 MB 图片；撤销/重做、另存为字节一致；保存重启、空附件目录恢复；文件缺失或写入失败不得虚报成功 |
| E08 | 查找替换、格式刷、清除格式 | 命中与替换范围正确，撤销可恢复 |
| E09 | AI 显隐、不同桌面窗口宽度、长文滚动 | 查找、清除格式、引用、代码、分隔线直接可用；选中图片显示宽度及文件夹入口；常用桌面宽度两行，窄窗口必要时换行，不重叠或截断；AI 显隐不挤动顶栏 |
| E10 | 正式构建中的扩展、插件和装饰对象兼容 | 无重复注册；选区、查找、代码高亮与表格缩放使用同一 DecorationSet；跨节点长/短/空替换、撤销/重做、图片及切换页面往返正常。不能仅凭 npm ls 已去重判断安全 |
| E11 | 粘贴网址，再选中并用另一个网址 Ctrl+V 覆盖 | 显示文字与实际链接地址一起替换，撤销/重做及重开正确；纯文本与网页富文本、普通文字/表格/待办均可替换；代码块保持纯文字；链接对话框仍可单独编辑地址。单击定位文字，Ctrl+单击打开链接。CUA 必须使用真实剪贴板验证 |
| E12 | 列表“⋯”/右键菜单，键盘打开、重命名、Enter 保存、Escape 取消、空名称 | 两个入口操作一致，菜单不超出窗口；侧栏、编辑标题和 AI 当前随记同步更新；改名与未保存正文互不覆盖；日期和顺序不变；改名其他随记不切走当前草稿；重开后名称保留；回收站中选择侧栏随记会回到正文 |
| A01 | AI 设置新增、测试连接、选择模型 | 可保存选择，失败有可理解信息；测试仅用本机模拟服务 |
| A02 | 当前随记/未命名随记/实时草稿生成 | 使用当前最新正文；无内容和仅图片不发请求 |
| A03 | 任务预览编辑、部分选中、加入待办 | 只创建确认项；责任人、日期、子任务正确；不重复加入 |
| A04 | 取消生成、切换随记、错误后重试 | 中止请求；旧响应不污染新随记；界面可恢复 |
| A05 | 文本模型、表格、清单、图片混合内容 | 请求只含文本，保留表格/完成标记，不发送图片字节 |
| A06 | JSON/温度/token 参数兼容、拒绝/截断/空结果 | 有界且仅针对明确不兼容的降级；不盲重试鉴权或限流 |
| S01 | 主题、字体大小 | 页面、弹窗、色盘都可读，重开持久化 |
| E13 | 插入时间、撤销/重做、保存重开 | 在光标处插入当前本地日期和时间，作为固定文字保存；焦点留在正文，随记归档日期不变 |
| S02 | 数据与附件存储位置、导入/导出 | 图片与普通文件统一在 attachments；data.json 仅引用；旧图迁移前完整备份、原 images 保留；网络图片与网页预览说明准确，导出包含附件 |
| S03 | 本地备份、定时备份、恢复 | 随记和标签完整；恢复往返一致；旧备份缺省字段不清空当前随记；校验失败或恢复前备份失败时不覆盖；每次恢复重新确认 |
| S04 | 远程同步设置及失败状态 | 未配置提示清晰；真实服务读写需独立测试端点，不使用生产空间 |
| S05 | 关于、感谢、版本检查 | 文案和版本正确；网络检查失败可恢复，不能伪称升级成功 |

## 可重复运行的自动化

在 `app` 目录依次运行，避免多个临时开发服务器争抢资源：

```powershell
npm run lint
npm run build
node scripts/test-ai-compat.mjs
npm run test:notes
node scripts/test-progress-save.mjs
node scripts/test-backup.mjs
node scripts/test-note-attachments.mjs
node scripts/test-task-workflow.mjs
node scripts/test-recurring.mjs
node scripts/test-ui-regressions.mjs
node scripts/test-note-workflow.mjs
./scripts/test-portable.ps1 -Executable '<交付 EXE 绝对路径>' -EditorWorkflow
./scripts/test-portable.ps1 -Executable '<交付 EXE 绝对路径>' -Attachments
```

待办联动、完成时间、排序及列表合并可单独运行 `node scripts/test-note-completion.mjs` 检查生产网页构建，或运行 `./scripts/test-portable.ps1 -Executable '<被测 EXE 绝对路径>' -NoteCompletion` 检查隔离原生数据的写入与重载。这两个入口还覆盖日历定位、跨日期新建、搜索/标签组合与清除、回收站计数及重载；`npm run test:notes` 包含编辑器回归。排序使用两个带文字的直接按钮“未完成在前”“已完成在前”，支持键盘操作，已排序时对应按钮禁用，1100px 窗口仍完整可见。删除间隔后相邻同级待办应合并，间距与单个列表一致；撤销恢复间隔，子项、格式和完成时间保持，仍有段落或位于不同父项/单元格的列表保持独立。

清除格式专项可运行 `node scripts/test-note-formatting.mjs`，原生版本使用 `./scripts/test-portable.ps1 -Executable '<被测 EXE 绝对路径>' -ClearFormatting`。覆盖不同字体/字号/行距粘贴后统一正文样式、软换行与段落的实际行距、部分选区、表格单元格、代码换行与保留空行、重复清除、独立撤销/重做、继续输入及保存重载；`npm run test:notes` 同样包含这些检查。普通粘贴保留格式，只有显式清除时才把选中的软换行统一为正文段落，不合并或删除原有空行。

原生工作流测试前退出其他 ProjectTodo 进程，避免单实例锁唤醒用户窗口。Computer-use 会话中窗口生命周期直接用其 API 操作；不要混用其他 Windows UI 自动化接口。

`-Attachments` 会依次弹出备份保存、备份打开和附件另存为原生对话框；用 Computer Use 或人工在 180 秒内输入脚本打印的隔离路径。脚本自动验证恢复前后文件哈希，Windows 资源管理器真实复制粘贴另行验收。

真实模型的提取质量、真实同步服务和不同 Windows/WebView 版本应独立记录覆盖情况。本机模拟响应只能证明传输、兼容与交互流程，不能证明所有供应商和模型质量。

## 原生关闭故障回归

在 `app` 目录运行 `./scripts/test-portable.ps1 -Executable <EXE路径> -WindowRecovery crash`，随后按脚本提示，用 Computer Use 或人工点击原生 X 和“退出程序”。再以 `-WindowRecovery busy` 运行，选择“继续等待”。脚本会验证正常关闭监听、原生超时、取消后不误退出、恢复后的内容及最终退出；全部使用隔离数据并核对原有数据哈希。

渲染崩溃测试验证关闭兜底，不等于已经查明用户设备上触发白屏的原因。不要把“禁用全部脚本后仍能关闭”当作对运行中崩溃的覆盖。
