# 随记编辑器与待办筛选：14 处批注检查

日期：2026-09-06。范围为本轮用户标注的 14 处界面及相关交互。沿用项目主题、Tiptap 编辑器与已安装图标库，无新增生产依赖。

## 批注处理

| 批注 | 改动与验证 |
| --- | --- |
| 1 | AI 设置使用 Lucide 齿轮，保留标题与无障碍名称。 |
| 2 | 工具栏按文字、编辑、段落、插入四组分配宽度，每组两排控件均匀排列。 |
| 3 | 分组说明跟随应用主题色，而不是固定灰色。 |
| 4 | 入口文字为“AI助手：一键生成待办事项”。 |
| 5 | 标题上方内边距由 24px 缩到 10px，标题自身间距同步减少。 |
| 6 | 字体、字号、标题、行距、列表、表格、图片宽度复用现代下拉组件；选中态、键盘操作、Escape 和视口边界定位统一处理。 |
| 7 | AI 面板默认展开，手动点击入口可以收起和展开。 |
| 8 | 读取尚未自动保存的最新正文；直接请求、真实取消、超时/失败重试；结果可编辑、选择并分批加入待办。关闭侧栏保留结果，切换随记取消旧请求。 |
| 9 | 待办搜索、筛选与设置改用组件图标，移除对应 emoji。 |
| 10–11 | 筛选与统计共享固定 53px 行；移除该行位移动画。开关两轮、筛选与清空后检查，任务列表位置不变。 |
| 12 | “关于”的特别鸣谢增加桂树奇。 |
| 13 | 链接编辑改用完整弹窗，显示文字、链接地址及底部按钮都在可视范围内；保留选中文本与编辑/移除链接能力。 |
| 14 | 图片改为“选择图片 → 预览 → 插入”，区分本地图片与网络链接。“图片说明（选填）”附通俗解释，显示存储说明并提供打开图片文件夹按钮。 |

复核 AI 流程另外修复两处行为问题：周日周期的日期偏移；刚加入今天到期的待办时，旧的启动提醒突然弹出。输入中的图片与 HTML 不再发送给任务提取模型。

## 图片存储

EXE 的本地上传、粘贴和拖入图片会保存到系统“文档 / ProjectTodo / images”；同时在笔记 HTML 中保留副本，维持既有备份、导出与旧笔记兼容性。文件按内容摘要命名，同一图片不会重复写文件。选中旧的内嵌图片后点击“打开图片文件夹”，也会先将该图片写入本地目录。

网页预览使用当前浏览器存储，无法直接打开 Windows 数据目录，因此明确说明并禁用插入窗口的目录按钮。网络图片继续引用原网址，说明其需要联网查看。

## 视觉证据

- 应用内浏览器复查使用用户批注的 1538 × 698 桌面 viewport。
- 当前源码的网页截图：`app/ui-check.local/annotations/`。
- 隔离回归截图：`app/ui-check.local/annotation-workflow-browser/`，包括工具栏下拉、AI 结果、完整链接窗口、图片窗口、紧凑筛选。
- 编辑器回归覆盖 1100、1280、1536、1920px 桌面宽度，并检查助手开关、控件重叠和视口溢出。
- 对照重点是用户标出的密度、位置、颜色和控件边界；保留紫色主题及现有应用结构。测试截图使用临时数据，不纳入源码。

## 已运行的检查

- `npm run lint`：通过。
- `npm run build`：通过，保留现有的分包大小与动态导入提示。
- `npm run test:notes`：通过；旧 HTML、列表、排版、表格、图片上传/粘贴/拖放、保存/重载/导出及多宽度布局。
- `node scripts/test-ui-regressions.mjs`：通过；颜色、退出、随记日期、草稿/自动保存、编号、回收站、旧数据迁移、撤销重做、虚拟滚动。
- `node scripts/test-ai-compat.mjs`：通过；原生 HTTP 传输、取消信号、模型兼容与连接验证。
- `node scripts/test-note-workflow.mjs`：通过；本轮 AI 交互、取消网络请求、结果筛选与编辑、周期日期、错误重试、链接、图片、筛选几何位置、键盘下拉、图标与鸣谢。
- `cargo check --locked`：通过。

## 发行验证

合入 main 后构建免安装 EXE，使用 `scripts/test-portable.ps1 -Executable <exe> -EditorWorkflow` 执行隔离启动及实际 WebView 工作流：通过原生 HTTP 请求本机模拟接口，并检查上传图片真实落盘、内容摘要及原有用户数据未改变。结果写入对应 `app/ui-check.local/portable-*/result.json`，随包说明记录最终提交与 SHA-256。

AI 流程使用本机模拟服务，未调用用户的真实付费接口；不将流程回归等同于实际模型提取准确率验证。