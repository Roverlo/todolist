# 随记顶部与助手侧栏设计检查

日期：2026-09-06。范围：现有 Windows 随记编辑器的顶部布局及助手展开、收起状态。

## 视觉依据与对照

用户提供的两张当前版本截图指出：工具栏右端留白较大，助手从工具栏下方开始。修改保留现有字体、紫色强调色、编辑功能和随记内容。

本次在应用内浏览器重新打开实际代码，用同一篇临时随记捕获前后状态。证据保存在当前功能 worktree 的 `app/ui-check.local/layout/`，不包含真实用户随记。

- Source visual truth：`before-open.jpg`、`before-closed.jpg`。
- Implementation：`after-open.jpg`、`after-closed.jpg`。
- 宽窗口前后截图均为 1536 × 784 像素，对应同尺寸 CSS viewport，密度 1；直接比较，无须缩放匹配。用户原始截图包含 Windows 标题栏及系统缩放，仅用于识别问题，未将其像素尺寸直接当作 CSS 尺寸。
- 最小窗口补充证据：`after-narrow-open.jpg`、`after-narrow-closed.jpg`，均为 1100 × 840 像素。
- 完整画面与顶部局部组合对照：`compare.html`、`comparison.jpg`。同一对照页包含前后完整画面及原尺寸顶部裁切，检查对象不是分开观看的旧截图。
- 状态一致：同一随记标题、正文和日期；助手展开和收起分别比较。光标所在段落与经过的保存时间属于正常编辑状态变化。

## 检查结果与修正记录

1. **助手展开：通过。** 工具栏限制在正文列，助手从工作区顶部开始，操作入口紧接助手标题，取消原来的大段顶部空白。原 P2 布局问题已解决。
2. **助手收起：通过。** 四个功能组横向使用正文宽度，分隔线和组名形成明确边界；原右端成片闲置区域已改为插入功能组。
3. **窄窗口：通过。** 内容列不足时工具组自动换行，保存、导出仍可见，侧栏切换保留正文。

迭代一发现字号/标题控件继承了组件库 96px 的最小宽度，导致字体组多换一行（P2）；在公共按钮样式中明确 `min-width: 0` 后重拍，928px 正文列的工具栏高度为约 101px，已恢复两排工具。

迭代二发现 1100px 窗口下旧媒体规则将侧栏轨道设为 230px，而侧栏实际为 260px（P2）；删除该重复规则，复查侧栏右边界为 260px，正文左边界为 270px，重叠消失。修正后的两种窄窗口截图已保存。

最终组合对照及窄窗口复查没有未解决的 P0/P1/P2 视觉问题。

## 五项视觉检查

- 字体：沿用中文字体栈、正文层级及工具图标，组名 11px；标题、字号和字体选择器没有截断。
- 间距：正文与助手标题栏顶部对齐；分组边界清晰；1100px 时允许增高换行，避免隐藏编辑功能。
- 颜色：保留紫色操作和选中态；助手背景改为浅灰，说明文字使用灰色，主操作取消渐变和阴影。这些是本次有意调整。
- 图像：继续使用现有图标库，未增加装饰图或用代码绘制替代资产。对比图来自真实浏览器捕获。
- 文案：随记正文、日期和助手说明不变，没有把实现细节放进用户界面。

## 交互验证与边界

`npm run lint`、`npm run test:notes`、`node scripts/test-ui-regressions.mjs` 均通过。编辑器测试涵盖四种桌面宽度、助手开关、更多菜单及 Escape、旧 HTML、列表、表格、图片、保存和重新加载。应用内浏览器另行检查了 AI 设置打开与关闭。

已查看浏览器控制台：存在原有的 Tauri 文件桥不可用日志，浏览器使用 localStorage 回退；这不是 Windows EXE 的原生文件保存验证。发行包另用 `app/scripts/test-portable.ps1` 在独立数据目录启动并检查，避免改动用户数据。未调用真实 AI 服务，也不将截图检查等同于完整无障碍认证。

final result: passed
